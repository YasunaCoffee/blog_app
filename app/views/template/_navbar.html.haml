/* navbar
%nav.navbar.navbar-light.navbar-customclass
  .container-fluid
  .bar-search
    = icon("fas", "bars", class: "bars-icon")
    = icon("fas", "search", class: "search-icon")
  =link_to root_path do
    .navbar-brand
      = icon("fas", "mug-hot", class: "coffee-icon")
      Fukusuke Coffee
  .like-cart
    - if user_signed_in?
      = link_to new_article_path do
        = icon("fas", "pen", class: "pen")
    -if user_signed_in?
      =link_to current_user do
        = icon("fas", "user-circle", class: "user-circle")
    -else
      =link_to new_user_session_path do
        = icon("fas", "user-circle", class: "user-circle")
    - if user_signed_in?
      = link_to destroy_user_session_path, method: :delete do
        = icon("fas", "sign-out-alt", class: "sign-out-alt")
